version: '3'

services:
  metabase:
    image: metabase/metabase
    container_name: metabase-ponderada
    ports:
      - "3000:3000"
    volumes:
      - ./metabase-data:/metabase.db
    restart: always
